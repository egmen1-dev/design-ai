# RFC-604 — Decision Consistency

DecisionGraph must satisfy:

```yaml
Consistency:

  Hero supports CommercialGoal

  Environment supports Hero

  Composition supports Environment

  Typography supports Hero

  Overlay supports Typography

  Lighting supports Environment

  Camera supports Product
```

Contradictory decisions invalidate DecisionGraph.
