# RFC-1504 — Cache Reuse

Scheduler SHALL reuse previous execution when inputs are unchanged.

Cache validity SHALL depend on:

```yaml
InputHash

KnowledgeVersion

DecisionVersion

SceneVersion

ProviderVersion
```
