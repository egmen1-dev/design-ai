# RFC-1200 — Event Driven Architecture

Every state mutation inside DAOS SHALL occur through Protocol Events.

Direct object mutation is prohibited.

Function calls SHALL NOT be considered system events.

Events are immutable.

Events are append-only.

Events are replayable.
