# RFC-1300 — Purpose

DAOS Protocol Language (DPL) SHALL be the native communication language for all autonomous subsystems.

Natural language SHALL NOT be used for internal reasoning.

JSON SHALL be considered only a serialization format.

Commercial reasoning SHALL be represented as protocol objects.
