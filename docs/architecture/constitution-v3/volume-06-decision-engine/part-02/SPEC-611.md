# SPEC-611 — Stage 1 — Context Initialization

The system initializes immutable generation context.

```yaml
GenerationContext:

  projectId

  runId

  marketplace

  category

  product

  language

  targetAudience

  commercialGoal

  generationMode
```

GenerationContext becomes immutable after initialization.
