# RFC-1207 — Canonical Mutation Rule

Inside DAOS:

NO OBJECT SHALL CHANGE WITHOUT A PROTOCOL EVENT.

The following are prohibited:

- Direct SceneGraph mutation.
- Direct DecisionGraph mutation.
- Direct Commercial Genome mutation.
- Hidden in-memory mutation.
- Function-local mutation outside protocol.

Every state change SHALL be represented by exactly one immutable Protocol Event.
