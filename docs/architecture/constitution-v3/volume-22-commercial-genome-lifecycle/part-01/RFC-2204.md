# RFC-2204 — Genome Rollback

Rollback SHALL never delete knowledge.

Rollback creates a new version with lower lifecycle state.

Example:

```text
stable
  ↓
certified
```

Rollback SHALL require:

```yaml
RegressionEvidence

HumanApproval

BenchmarkReference

RollbackReason
```
