# RFC-700 — Unified Agent Reasoning

Every autonomous agent inside DAOS must follow the same reasoning protocol.

Reasoning style is standardized.

Decision quality must not depend on the individual implementation of an agent.

Every expert reasons using the same sequence.

Only domain knowledge differs.
