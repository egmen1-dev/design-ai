# RFC-1600 — DAOS Kernel

DAOS Kernel SHALL be the immutable execution core of the platform.

Every subsystem SHALL execute under Kernel supervision.

Kernel SHALL own lifecycle control.

Kernel SHALL own protocol validation.

Kernel SHALL own compatibility guarantees.

Commercial reasoning SHALL remain outside the Kernel.
