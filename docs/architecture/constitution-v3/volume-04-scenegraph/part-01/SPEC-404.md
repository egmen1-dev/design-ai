# SPEC-404 — Overlay Node

Overlay is represented as structured elements.

```yaml
OverlayNode:

  elements:

  density:

  visualWeight:

  overlap:

  readability:

  contrast:
```

Overlay nodes never calculate geometry independently.

Overlay reads ProductNode.actual.
