# SPEC-1706 — Plugin ABI

Every plugin SHALL expose:

```yaml
initialize()

shutdown()

validate()

execute()

subscribe()

publish()

diagnostics()
```

Optional methods require explicit declaration.
