# RFC-1700 — DAOS Module System

Every extensible subsystem inside DAOS SHALL be implemented as a Plugin.

Core Kernel SHALL remain unaware of plugin internals.

Plugins SHALL communicate only through Kernel Protocol.

Plugins SHALL NEVER communicate directly.
