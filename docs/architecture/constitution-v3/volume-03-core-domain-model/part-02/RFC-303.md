# RFC-303 — Decision Model

Commercial design inside DAOS is represented as structured decisions.

DAOS never decides directly inside rendering modules.

Every commercial choice must exist as a Decision object before execution.

Decision objects are immutable after approval.
