# RFC-2506 — Context Traceability

Every Decision SHALL reference:

```yaml
generationContextId
```

Every SceneGraph SHALL reference:

```yaml
generationContextId
```

Every Benchmark SHALL reference:

```yaml
generationContextId
```

Context SHALL remain fully traceable.
