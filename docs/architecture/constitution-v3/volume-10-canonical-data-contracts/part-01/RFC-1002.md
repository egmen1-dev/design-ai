# RFC-1002 — Canonical Object Rule

Every logical concept SHALL exist exactly once.

Forbidden examples:

```text
ProductBounds
↓
ProductPlacement
↓
ProductBox
↓
HeroBounds
↓
ObjectBounds
↓
SubjectBounds
```

All SHALL be represented by

```yaml
ProductNode.actual.bounds
```

Canonical representation SHALL always replace aliases.
