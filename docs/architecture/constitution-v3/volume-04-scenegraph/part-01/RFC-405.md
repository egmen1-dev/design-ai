# RFC-405 — Legacy Objects

Legacy objects remain compatibility adapters.

Examples:

- compositionLayout
- layoutSpec
- renderBlueprint
- infographicData

These objects shall gradually become projections generated from SceneGraph.

SceneGraph must never depend on them.
