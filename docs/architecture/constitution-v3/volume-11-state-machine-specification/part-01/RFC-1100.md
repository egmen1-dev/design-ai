# RFC-1100 — System State Machine

DAOS SHALL be modeled as a deterministic finite state machine.

Every generation SHALL exist in exactly one lifecycle state.

Parallel lifecycle states are prohibited unless explicitly declared.

Undefined states are prohibited.
