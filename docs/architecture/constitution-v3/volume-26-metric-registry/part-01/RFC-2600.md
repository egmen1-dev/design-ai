# RFC-2600 — Purpose

Every measurable value inside DAOS SHALL be represented by exactly one canonical metric.

Metrics SHALL be globally registered.

Metrics SHALL NEVER exist as anonymous calculations.
