# RFC-400 — SceneGraph

SceneGraph is the single authoritative representation of the commercial design state.

Every rendering operation, audit, benchmark, governance rule and learning process MUST consume SceneGraph.

No subsystem may maintain an alternative representation of the scene.

No subsystem may derive geometry independently when equivalent SceneGraph data exists.
