# RFC-1902 — Single Source of Truth

Every logical entity SHALL have exactly one canonical owner.

Examples:

```yaml
Decision:
    DecisionGraph

Scene:
    SceneGraph

Knowledge:
    CommercialGenome

State:
    StateMachine

Events:
    EventBus
```

Aliases SHALL NOT become authoritative.
