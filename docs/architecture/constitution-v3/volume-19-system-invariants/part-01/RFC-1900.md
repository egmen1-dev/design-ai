# RFC-1900 — Global Invariants

System Invariants define properties that SHALL remain true during the entire lifecycle of every generation.

Violation of an invariant SHALL invalidate the generation.

No subsystem MAY override a System Invariant.
