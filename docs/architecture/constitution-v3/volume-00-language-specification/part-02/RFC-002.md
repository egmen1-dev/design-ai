# RFC-002 — Agent Communication Contract

Every autonomous agent inside DAOS must satisfy:

```yaml
AgentContract:

  input:

    protocol_objects_only

  output:

    protocol_objects_only

  memory:

    immutable

  communication:

    DAOS Protocol

  logging:

    structured

  free_text:

    forbidden
```
