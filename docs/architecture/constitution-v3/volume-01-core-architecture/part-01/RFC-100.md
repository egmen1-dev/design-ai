# RFC-100 — Core Architecture

DAOS is composed of independent deterministic engines.

Each engine owns exactly one domain.

Ownership overlap is forbidden.

Each engine exposes a public contract.

Each engine consumes protocol objects.

Each engine produces protocol objects.

No engine may directly mutate another engine's internal state.
