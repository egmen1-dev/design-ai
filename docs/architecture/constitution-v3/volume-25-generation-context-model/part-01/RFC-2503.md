# RFC-2503 — Context Immutability

GenerationContext SHALL NOT change after initialization.

Derived objects SHALL be created separately.

SceneGraph SHALL NOT modify GenerationContext.

DecisionGraph SHALL NOT modify GenerationContext.
