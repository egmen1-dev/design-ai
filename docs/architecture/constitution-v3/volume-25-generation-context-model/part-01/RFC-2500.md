# RFC-2500 — Purpose

GenerationContext SHALL describe everything required to perform one commercial generation.

GenerationContext SHALL remain immutable after initialization.

Every subsystem SHALL consume the same GenerationContext.

Local context reconstruction is prohibited.
